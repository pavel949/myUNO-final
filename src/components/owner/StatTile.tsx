// Re-export of the shared StatTile — kept so existing owner imports keep
// working; the single implementation lives in src/components/StatTile.tsx.
export { StatTile } from '@/components/StatTile';
